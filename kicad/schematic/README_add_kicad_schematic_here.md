# KiCad Schematic Folder

Place the following files here after drawing the schematic in KiCad:

- `emergency_event_counter.kicad_sch` — the KiCad schematic file
- `emergency_event_counter.kicad_pro` — the KiCad project file

Refer to `kicad/README.md` for the full list of components, net labels, and the ERC checklist.
